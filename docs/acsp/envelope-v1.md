# Envelope v1

Canonical JSON Schema: `gax/schemas/envelope.v1.json`

Required fields: `v`, `ok`, `cmd`, `audit_id`, `surface`, `data`, `meta`

Optional: `schema`, `error`, `next`

See `research/04-protocol-envelope.md` for surfaces and exit codes.
